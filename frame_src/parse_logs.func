#!/bin/bash

# $1 -- log file
# $2 -- result file
parse_unixbench()
{
    local tmpdir=$(mktemp -d)
    local nr_rcd=$(grep "Benchmark Run" $1 | wc -l)
    cp -avf $1 $tmpdir

    splite_file "Benchmark Run:" "unixbench test at" "$1" "$tmpdir" || return 1
    for i in $(seq $nr_rcd)
    do 

    done 
}
